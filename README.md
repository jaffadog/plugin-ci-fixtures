# fixture: good-plugin

Baseline fixture. A minimal, fully valid Signal K plugin with no dependencies, a real
(trivial) test, and no anti-patterns. **Expected result: fully green**, including the
armv7 (advisory) job.

See the [main branch README](https://github.com/jaffadog/plugin-ci-fixtures/blob/main/README.md)
for how this repo's branch-per-fixture scheme works.
