{
  "name": "signalk-plugin-ci-fixture-api-misuse",
  "version": "1.0.0",
  "description": "plugin-ci.yml fixture: accesses four internal properties flagged by the misuse (error) tier of the API-usage scan.",
  "main": "index.js",
  "scripts": {
    "test": "node --test"
  },
  "keywords": ["signalk-node-server-plugin"],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0"
}
