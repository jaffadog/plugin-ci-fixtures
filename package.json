{
  "name": "signalk-plugin-ci-fixture-malicious-postinstall",
  "version": "1.0.0",
  "description": "plugin-ci.yml fixture: a postinstall script that must never execute anywhere in the pipeline.",
  "main": "index.js",
  "scripts": {
    "postinstall": "node -e \"require('fs').writeFileSync('postinstall-ran.marker','yes')\"",
    "test": "node --test"
  },
  "keywords": ["signalk-node-server-plugin"],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0"
}
