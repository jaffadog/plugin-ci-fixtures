{
  "name": "signalk-plugin-ci-fixture-native-required",
  "version": "1.0.0",
  "description": "plugin-ci.yml fixture: a REQUIRED (not optional) native dependency — must fail the static App Store scan directly, not just the reinstalled test run.",
  "main": "index.js",
  "scripts": {
    "test": "node --test"
  },
  "keywords": ["signalk-node-server-plugin"],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1"
  },
  "license": "Apache-2.0"
}
