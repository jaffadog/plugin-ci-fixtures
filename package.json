{
  "name": "signalk-plugin-ci-fixture-api-usage-warnings",
  "version": "1.0.0",
  "description": "plugin-ci.yml fixture: triggers all 11 warning-tier patterns in the API-usage scan (deprecated APIs, direct Express routes, file-storage anti-patterns, security anti-patterns).",
  "main": "index.js",
  "scripts": {
    "test": "node --test"
  },
  "keywords": ["signalk-node-server-plugin"],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0"
}
