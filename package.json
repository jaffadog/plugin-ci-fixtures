{
  "name": "signalk-plugin-ci-fixture-stray-files",
  "version": "1.0.0",
  "description": "plugin-ci.yml fixture: the test script writes an untracked file as a side effect.",
  "main": "index.js",
  "scripts": {
    "test": "node -e \"require('fs').writeFileSync('stray-debug-output.log', 'oops')\" && node --test"
  },
  "keywords": ["signalk-node-server-plugin"],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0"
}
